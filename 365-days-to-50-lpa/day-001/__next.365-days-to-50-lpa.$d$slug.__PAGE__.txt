1:"$Sreact.fragment"
2:I[22016,["/_next/static/chunks/18f38dbeee9f54d3.js","/_next/static/chunks/d87b716920d553cc.js"],""]
3:I[5500,["/_next/static/chunks/18f38dbeee9f54d3.js","/_next/static/chunks/d87b716920d553cc.js"],"Image"]
9:I[97367,["/_next/static/chunks/ff1a16fafef87110.js","/_next/static/chunks/bf1f6d5df2e9b1a8.js"],"OutletBoundary"]
a:"$Sreact.suspense"
4:T4dc2,<h2>What we will do today</h2>
<p>We begin our transition toward Senior/Staff Mobile Engineering with full-stack and applied-AI capabilities by creating a place to work, choosing one available AI assistant, and completing one small exercise. By the end of this chapter, we will have a local folder, a JavaScript file we have run, and a journal explaining our decisions and starting knowledge.</p>
<p>This chapter is a plan we can follow. Its sample answers and results are illustrative; our actual results belong in our own journal after we perform the exercise.</p>
<p>We already bring different strengths to the journey. Some of us have delivered React Native applications for years. Others are still learning JavaScript. Today’s exercise gives us a small starting measurement we can revisit. It does not assess our entire engineering ability or determine our readiness for a job title.</p>
<h2>Our workspace and assistant</h2>
<p>A <strong>workspace</strong> is simply a folder containing the files for our work. We can open it in a code editor and keep the exercise beside the explanation of why we wrote it.</p>
<p>An <strong>AI assistant</strong> receives our instructions and produces suggestions. A coding agent may also have tools that read files, edit code, or execute commands. We will start with an assistant we already have access to. A browser chat is sufficient for today: we can copy a short snippet into it and apply reviewed suggestions ourselves.</p>
<p>We will use that assistant in two clearly separated roles:</p>
<ul>
<li><strong>Planning assistant:</strong> helps us create a short checklist and understand unfamiliar steps.</li>
<li><strong>Review assistant:</strong> challenges our explanation and suggests tests after our first attempt.</li>
</ul>
<p>These are two scoped conversations or tasks. We do not need two products or a custom multi-agent system. We will build and understand more capable agents later in the journey.</p>
<p>If a coding agent is already installed, we use its review or approval controls and limit it to this new folder. For today, it needs no access to our employer’s repositories, cloud accounts, or public profiles.</p>
<h3>What happens to Ollama, Hermes, and the coding CLI?</h3>
<p>We record their current status today and use one available assistant during Week 1. In Week 2, we select our primary coding workflow and benchmark one local model with Ollama if our hardware supports it. In Week 3, we apply that workflow to our first mobile screen. In Week 4, we prepare evidence drafts with Hermes or our existing assistant; manual publication remains sufficient. This follows the <a href="/365-days-to-50-lpa/schedule/">complete schedule</a>.</p>
<p>If a tool is already available, we record what we actually use. If it is absent, we write “not configured.” We do not need to install all the candidates before beginning.</p>
<h2>The 90-minute session</h2>
<p>Reading and troubleshooting belong inside the time budget. We stop when the session ends and record anything unfinished.</p>
<table>
<thead>
<tr>
<th>Minutes</th>
<th>Our activity</th>
</tr>
</thead>
<tbody>
<tr>
<td>0–15</td>
<td>Read the chapter and identify our editor and available assistant.</td>
</tr>
<tr>
<td>15–30</td>
<td>Create the folder and journal; check whether Node.js is available.</td>
</tr>
<tr>
<td>30–45</td>
<td>Predict and run the small exercise; save our original explanation.</td>
</tr>
<tr>
<td>45–60</td>
<td>Ask the review assistant for feedback and apply one focused correction.</td>
</tr>
<tr>
<td>60–75</td>
<td>Run the checks and explain the corrected code in our own words.</td>
</tr>
<tr>
<td>75–90</td>
<td>Record our tool decisions, one gap, and a public-update draft.</td>
</tr>
</tbody>
</table>
<p>If setup takes the full first 30 minutes, we use a browser console for the exercise and defer Node.js installation. We can also save our files locally if Git or GitHub is not ready. Repository setup and publication can continue during Week 1.</p>
<h2>Step 1: Create a place for the journey</h2>
<p>Using our file manager, we create a folder named <code>365-day-journey</code> in a location we can find again. Inside it, we create a <code>day-001</code> folder. We open that folder in our preferred code editor and create two files:</p>
<pre><code class="language-text">365-day-journey/
  day-001/
    journal.md
    baseline.js
</code></pre>
<p>The <code>.md</code> extension means <strong>Markdown</strong>, a plain-text format that uses simple characters for headings and lists. For example, <code># Day 1</code> becomes a heading when displayed by GitHub or our website. We can edit it as ordinary text.</p>
<p>The <code>.js</code> extension identifies a JavaScript file. It will hold a tiny function and its checks.</p>
<p>We put this template in <code>journal.md</code>:</p>
<pre><code class="language-markdown"># Day 1 — Our starting point

## Session
- Date:
- Minutes spent:
- Status: in progress

## Environment
- Operating system:
- Editor:
- JavaScript runtime and version, or browser:
- Assistant and model, if shown:
- Assistant access: chat only / approved workspace tools

## Tool decisions
- What we used:
- Why it was sufficient today:
- Alternatives deferred and why:
- Assumptions or limitations:
- What would make us reconsider:

## Exercise
- Our prediction before running:
- Actual output:
- Our explanation before AI feedback:
- Assistant suggestions:
- What we accepted or rejected, and why:
- Checks we actually ran:
- What we can now explain:
- What remains unclear:

## Visibility
- GitHub: existing / not configured
- Notion: existing / not configured
- Public site or LinkedIn: existing / not configured
- Public-update draft:
- Publication status: draft, not published

## Next step
- One gap to revisit:
- One action for Day 2:
</code></pre>
<p>We fill this progressively as we work. Empty fields are acceptable while the session is in progress. The template is a working document, not a completed account of our experience.</p>
<p>For a planning assistant, we can use this prompt:</p>
<pre><code class="language-text">We have 90 minutes for Day 1 of our engineering journey.
Help us follow this chapter using one editor, one assistant,
one JavaScript file, and one journal.

Ask which tools we already have. Explain unfamiliar steps plainly.
Give us a short checklist. Keep the exercise answers for later.
Do not invent our environment, results, or completed work.
</code></pre>
<h2>Step 2: Check how we will run JavaScript</h2>
<p>JavaScript needs a <strong>runtime</strong>: a program that executes JavaScript instructions. A browser includes one. Node.js also runs JavaScript, including from files outside a browser.</p>
<p>If a terminal is already available in our editor, we enter:</p>
<pre><code class="language-sh">node --version
</code></pre>
<p>This asks Node.js to print its installed version. We copy the actual output into the journal. If the command is unavailable, we record that and use the browser route below. We do not spend the session troubleshooting installation.</p>
<p>With Node.js available, we open the terminal in the <code>day-001</code> folder. After saving code in <code>baseline.js</code>, we can run it with:</p>
<pre><code class="language-sh">node baseline.js
</code></pre>
<p>If we get a “cannot find module” or file-not-found message, we check that the terminal is in <code>day-001</code> and that the filename is exactly <code>baseline.js</code>.</p>
<h3>Browser route</h3>
<p>We open a blank browser tab, open its developer tools, and select the <strong>Console</strong> panel. The console can evaluate JavaScript we type. We enter the small examples from this chapter there and keep the same code in our local file for reference. If the browser restricts pasting, we type the short example ourselves.</p>
<p>When repeating declarations causes an “already declared” error, a fresh blank tab gives us a fresh console. Our journal records that we used a browser rather than Node.js.</p>
<h2>Step 3: Capture our first explanation</h2>
<p>Our exercise connects something familiar—a name displayed on a screen—to a backend concern: data may have a different shape from what our interface expects.</p>
<p>An <strong>API</strong> is an interface through which software exchanges requests and responses. A mobile application often receives an object from an API and turns its fields into visible text.</p>
<p>We start with this function:</p>
<pre><code class="language-js">function greeting(user) {
  return "Hello, " + user.name.toUpperCase();
}

console.log(greeting({ name: "Asha" }));
console.log(greeting({ name: null }));
</code></pre>
<p><code>greeting</code> accepts an object named <code>user</code>. It reads the <code>name</code> property and calls <code>toUpperCase()</code>, a method that converts a string to uppercase. <code>console.log()</code> prints a result.</p>
<p>Before running the code, we write two predictions in our journal:</p>
<ol>
<li>What will the first call print?</li>
<li>What will happen when <code>name</code> is <code>null</code>, meaning that no value is present?</li>
</ol>
<p>We then run the code and preserve our actual output. We write a short explanation before consulting the assistant. If we do not know the answer, “I do not yet understand this behaviour” is useful evidence.</p>
<p><strong>Checkpoint:</strong> we save our prediction and first output before reading the worked explanation below. Reading the explanation first is fine for learning; we simply label that attempt “guided” in our journal.</p>
<h2>Step 4: Work through the failure</h2>
<p>The first call prints:</p>
<pre><code class="language-text">Hello, ASHA
</code></pre>
<p>The second call throws a <code>TypeError</code>. Its exact wording depends on the runtime, but the reason is that <code>null</code> has no <code>toUpperCase</code> method.</p>
<p>This is a useful failure. The function assumes <code>name</code> is a string without checking that assumption. In a real product, a similar problem might come from an API response, stored data, or an AI-generated answer.</p>
<p>TypeScript helps us reason about code through types, but a type declaration alone does not inspect incoming network data while the program runs. We will study that boundary more deeply when we build our backend services. Today we can make it visible with a small JavaScript check.</p>
<h3>Define the behaviour before changing the code</h3>
<p>We choose a simple contract for this exercise:</p>
<ul>
<li>A non-empty string name produces an uppercase greeting.</li>
<li>Spaces around a valid name are removed.</li>
<li>Missing, empty, or non-string names produce <code>Hello, friend</code>.</li>
</ul>
<p>A <strong>contract</strong> describes the behaviour callers can expect. Another product might reject invalid input instead. Our decision today is to display a friendly fallback, and our journal records why.</p>
<p>We ask the review assistant:</p>
<pre><code class="language-text">Act as our review assistant for this small JavaScript exercise.
We will provide the code, our original explanation, and actual output.

First, ask one question that checks our understanding of the failure.
Then suggest the smallest change meeting this contract:
- Trim and uppercase a non-empty string name.
- Return "Hello, friend" for missing, empty, or non-string names.

Explain each condition and suggest three edge cases.
Do not claim that tests passed unless we provide execution results.
We will decide what to accept and run the checks ourselves.
</code></pre>
<p>We keep its suggestions separate from our original answer. If it introduces a library or rewrites the whole example, we ask how that helps this specific contract.</p>
<h2>Step 5: Make and verify one correction</h2>
<p>Here is a worked correction we can compare with the assistant’s proposal:</p>
<pre><code class="language-js">function greeting(user) {
  if (user === null || typeof user !== "object") {
    return "Hello, friend";
  }

  if (typeof user.name !== "string") {
    return "Hello, friend";
  }

  const name = user.name.trim();

  if (name.length === 0) {
    return "Hello, friend";
  }

  return "Hello, " + name.toUpperCase();
}
</code></pre>
<p>The first condition checks whether a usable object was provided. We check <code>null</code> explicitly because JavaScript reports <code>typeof null</code> as <code>"object"</code>.</p>
<p>The second condition checks the property we need. An object can exist while its <code>name</code> is missing, a number, or <code>null</code>.</p>
<p>Only after confirming that <code>name</code> is a string do we call <code>trim()</code>. This removes surrounding whitespace. We then handle an empty result before constructing the greeting.</p>
<p>We replace the original function and calls in <code>baseline.js</code> with the corrected function, followed by these checks:</p>
<pre><code class="language-js">const cases = [
  [{ name: "Asha" }, "Hello, ASHA"],
  [{ name: "  Asha  " }, "Hello, ASHA"],
  [{ name: "   " }, "Hello, friend"],
  [{ name: null }, "Hello, friend"],
  [{ name: 42 }, "Hello, friend"],
  [{}, "Hello, friend"],
  [null, "Hello, friend"],
  [undefined, "Hello, friend"],
];

for (const [input, expected] of cases) {
  const actual = greeting(input);

  if (actual !== expected) {
    throw new Error("Expected: " + expected + "; received: " + actual);
  }
}

console.log("Passed " + cases.length + " checks.");
</code></pre>
<p>Each entry pairs an input with its expected output. The loop calls our function and compares the result. A mismatch throws an error; reaching the final line means all listed comparisons succeeded.</p>
<p>The expected output for this worked example is:</p>
<pre><code class="language-text">Passed 8 checks.
</code></pre>
<p>We record what our own run actually prints. These checks cover the contract we chose for a small exercise; they do not prove that we have built a complete API validation system.</p>
<p>To check our understanding, we explain why <code>trim()</code> comes after the type check. We also add one case of our own and predict the result before running it.</p>
<h2>Step 6: Record capability and assistance separately</h2>
<p>We avoid giving ourselves a single score for “backend engineering” based on this function. Instead, we record what this particular exercise demonstrated.</p>
<table>
<thead>
<tr>
<th>Dimension</th>
<th>What we record</th>
</tr>
</thead>
<tbody>
<tr>
<td>Understanding</td>
<td>Can we explain the failure and each condition?</td>
</tr>
<tr>
<td>Verification</td>
<td>Which cases did we actually run, and what happened?</td>
</tr>
<tr>
<td>Debugging</td>
<td>Can we investigate a failed case and explain a correction?</td>
</tr>
<tr>
<td>Assistance</td>
<td>Which suggestions or worked examples did we use?</td>
</tr>
<tr>
<td>Remaining gap</td>
<td>What do we still need help understanding?</td>
</tr>
</tbody>
</table>
<p>Using AI or documentation does not reduce demonstrated capability by itself. What matters is whether we can judge the suggestion, verify it, and take responsibility for the result. Following a worked example is a valid first step; we record it accurately so later comparisons remain useful.</p>
<p>Here is an <strong>illustrative journal excerpt</strong>, not a claim about our own session:</p>
<pre><code class="language-text">Original prediction: I expected the second call to print "Hello, NULL".
Observed: It threw a TypeError.
Review feedback: The assistant asked whether null is a string.
Accepted change: Check the input and name before calling string methods.
Rejected change: Adding a validation package for this tiny exercise.
Verification: Ran the eight supplied cases and one new missing-name case.
Understanding: I can explain why the string check precedes trim().
Gap: I want to understand how runtime validation fits with TypeScript types.
</code></pre>
<h2>Step 7: Record tools, decisions, and visibility</h2>
<p>Our journal should name the editor, runtime, assistant, and model we actually used. If the model name is not visible, we write “not shown.” We also record whether the assistant only saw pasted text or could act inside our workspace.</p>
<p>For each material choice, we record the option selected, why it fits today, an alternative we deferred, how we checked it, and what would make us reconsider. For example:</p>
<pre><code class="language-text">Decision: Use the existing chat assistant for Day 1.
Reason: It can review this small snippet with no additional setup.
Deferred alternative: A local coding agent, to evaluate during Week 2.
Limitation: We copy suggestions and execution output manually.
Verification: We ran the accepted code locally and inspected the result.
Revisit: When workspace editing or repeatable local-model use becomes useful.
</code></pre>
<p>We also inventory our GitHub, Notion, and public profile status. GitHub will hold versioned work; Notion can provide a readable dashboard; the website and LinkedIn can help others discover our evidence. Today, recording existing links and preparing one draft is enough. Account setup and automation have their own scheduled sessions.</p>
<p>We can ask a documentation assistant to prepare a short update from the journal:</p>
<pre><code class="language-text">Using only the completed work in this journal, draft a short Day 1 update.
Include what we attempted, what failed, what we changed, what we verified,
and our next question. Preserve uncertainty. Do not invent scores,
screenshots, repository links, or successful checks. Leave it as a draft.
</code></pre>
<p>Before sharing, we check that each claim matches our results and that the draft contains only material we intend to make public. Our example uses fictional names and standalone code, so there is no reason to include employer code or account credentials.</p>
<p>If Git is already configured, we may commit the two files through our normal workflow after reviewing them. Otherwise, saving them locally completes today’s storage requirement. Git records changes and author-supplied timestamps; it does not independently certify when or by whom a task was completed.</p>
<h2>Our stopping point</h2>
<p>At 90 minutes, we save our files and mark the session accurately. A useful Day 1 result contains:</p>
<ul>
<li>A workspace and journal naming the tools we used.</li>
<li>Our original prediction and actual exercise output.</li>
<li>A reviewed correction with the checks we ran, or a specific unresolved blocker.</li>
<li>One explanation in our own words and one gap to revisit.</li>
<li>A decision entry and a public-update draft labelled with its actual status.</li>
</ul>
<p>If something failed, we record the error and the next diagnostic step. That makes the session useful without claiming that unfinished work succeeded.</p>
<h2>Questions we may have</h2>
<h3>Do we need to install every AI tool today?</h3>
<p>We need one usable assistant. We record other tools as existing, deferred, or not configured, then evaluate them in their scheduled sessions.</p>
<h3>What if JavaScript is new to us?</h3>
<p>We can follow the worked example, record unfamiliar terms, and finish with a guided result. We may need additional foundation sessions before following the experienced-engineer pace.</p>
<h3>Does passing these checks mean we know backend engineering?</h3>
<p>It demonstrates one small input-handling exercise. Broader capability requires more varied implementation, debugging, design, and operational evidence across the journey.</p>
<h3>What comes next?</h3>
<p>In <a href="/365-days-to-50-lpa/day-002/">Day 2</a>, we connect our starting experience and gaps to a target-role scorecard. We now have a concrete example of the daily process we will repeat: define behaviour, use assistance, inspect the result, verify it, and record the reasoning.</p>
0:{"buildId":"U0mW6QUe0o5InImLE9kXH","rsc":["$","$1","c",{"children":[["$","main",null,{"className":"d365-subpage d365-note-page","children":["$","article",null,{"className":"d365-note-shell","children":[["$","nav",null,{"className":"d365-note-topnav","aria-label":"Journey navigation","children":[["$","$L2",null,{"className":"d365-back-link","href":"/365-days-to-50-lpa/schedule","children":"← Complete schedule"}],["$","$L2",null,{"className":"d365-back-link","href":"/365-days-to-50-lpa","children":"Journey home"}]]}],["$","header",null,{"className":"d365-note-header","children":["$","div",null,{"className":"d365-note-heading","children":[["$","div",null,{"children":[["$","p",null,{"className":"d365-kicker","children":["Day ",1," · Week ",1]}],["$","h1",null,{"children":"Day 1 — Set Up Our Workspace and Capture Our Starting Point"}],["$","p",null,{"className":"d365-note-date","children":["Scheduled session · ","Monday, 14 September 2026"]}],["$","p",null,{"className":"d365-note-date","children":"Chapter · Planned exercise. Publication does not indicate completed work."}]]}],["$","$L3",null,{"className":"d365-note-portrait","src":"/images/instructor_image_pencil_transparent.png","alt":"Subrata Kumar Das","width":1092,"height":1379}]]}]}],["$","section",null,{"className":"d365-note-content","aria-labelledby":"session-plan-heading","children":[["$","h2",null,{"id":"session-plan-heading","children":"Our planned session"}],["$","p",null,{"children":"We set up our workspace, establish one assistant, and verify a small JavaScript baseline exercise."}],[["$","h3",null,{"children":"Weekly outcome"}],["$","p",null,{"children":"A workspace, starting exercise, and agreed first product slice."}],["$","h3",null,{"children":"Readiness check"}],["$","p",null,{"children":"We need an editor and one available assistant; Day 1 includes a browser fallback for JavaScript."}],false,["$","h3",null,{"children":"Human approval gate"}],["$","p",null,{"children":"We run the Day 1 checks and approve a brief identifying the user, problem, first feature, and non-goals."}],["$","p",null,{"children":["$","$L2",null,{"href":"/365-days-to-50-lpa/schedule#week-1","children":["View Week ",1,": agent roles, decisions, and evidence →"]}]}]]]}],["$","div",null,{"className":"d365-note-content","dangerouslySetInnerHTML":{"__html":"$4"}}],"$L5","$L6"]}]}],["$L7"],"$L8"]}],"loading":null,"isPartial":false}
5:["$","nav",null,{"className":"d365-day-pagination","aria-label":"Day navigation","children":[["$","span",null,{}],["$","$L2",null,{"href":"/365-days-to-50-lpa/day-002","children":["Day ",2," →"]}]]}]
6:["$","script",null,{"type":"application/ld+json","dangerouslySetInnerHTML":{"__html":"{\"@context\":\"https://schema.org\",\"@type\":\"Article\",\"headline\":\"Day 1 — Set Up Our Workspace and Capture Our Starting Point\",\"description\":\"We create a journey workspace, establish one AI assistant, and complete a small JavaScript exercise with human verification and a reusable decision journal.\",\"datePublished\":\"2026-09-13\",\"dateModified\":\"2026-09-13\",\"mainEntityOfPage\":\"https://subraatakumar.com/365-days-to-50-lpa/day-001\",\"url\":\"https://subraatakumar.com/365-days-to-50-lpa/day-001\",\"author\":{\"@type\":\"Person\",\"name\":\"Subrata Kumar Das\",\"url\":\"https://subraatakumar.com/\"},\"isPartOf\":{\"@type\":\"CreativeWorkSeries\",\"name\":\"365 Days to Senior/Staff Mobile Engineering with Full-Stack and Applied AI\",\"url\":\"https://subraatakumar.com/365-days-to-50-lpa\"}}"}}]
7:["$","script","script-0",{"src":"/_next/static/chunks/d87b716920d553cc.js","async":true}]
8:["$","$L9",null,{"children":["$","$a",null,{"name":"Next.MetadataOutlet","children":"$@b"}]}]
b:null
